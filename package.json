{
  "name": "lava-prompt-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Lava Prompt Lab Capacitor Android app",
  "scripts": {
    "build": "mkdir -p www && cp -f index.html styles.css app.js www/",
    "cap:sync": "npx cap sync android",
    "cap:copy": "npx cap copy android",
    "android:add": "npx cap add android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0"
  }
}
